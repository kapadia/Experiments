// Generated by CoffeeScript 1.6.0
(function() {
  var DOMReady, connect, connections;

  connections = {};

  connect = function() {
    var peer;
    peer = new Peer("http://webrtcb.jit.su:80", {
      binaryType: 'arraybuffer',
      video: false,
      audio: false
    });
    peer.listen();
    return peer.onconnection = function(connection) {
      connections[connection.id] = connection;
      return console.log(connections);
    };
  };

  DOMReady = function() {
    var button;
    console.log('DOMContentLoaded');
    button = document.querySelector("button[name='connect-to-peer']");
    return button.onclick = connect;
  };

  window.addEventListener('DOMContentLoaded', DOMReady, false);

}).call(this);
